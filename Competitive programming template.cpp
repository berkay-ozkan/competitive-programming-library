// #pragma GCC optimize("O3,unroll-loops")
// #pragma GCC target("avx2")
#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // cout << setprecision(numeric_limits<long_double> :: digits10 + 1);

    int cases;
    cin >> cases;

    while (cases--)
    {
        int n;
        cin >> n;

        // Reminder to carry out a binary search on the possible result range.
        // Reminder to limit the number of operations instead of trying to reduce the asymptotic complexity.
        int result = 0;
        cout << result << '\n';
    	// cout << result << endl;
    }
    return 0;
}
